## 무엇을 했나

<!-- 예: G20 열 5점 완료 -->

## 체크리스트

- [ ] CSV 컬럼이 정확히 `run_id,stress_GPa,mobility_gain_pct` 이다
- [ ] `run_id` 가 `G30_R50` 형식이다 (`G{Ge조성%}_R{리세스깊이_nm}`)
- [ ] `python analysis/build.py` 를 돌려 경고가 없었다
- [ ] `.tdr` / `.plt` 같은 대용량 원본을 커밋하지 않았다
- [ ] `runs/attachments/<run_id>/` 에 소자 사진·notes.md 를 올렸다 (공동 과제면 생략 가능)
- [ ] `baseline/` 을 건드리지 않았다 (건드렸다면 → 다른 팀원 2명 리뷰 필요)

## 결과 요약

| Run ID | stress_GPa | mobility_gain_pct |
|---|---|---|
|  |  |  |

## 리뷰어가 봐줬으면 하는 것

<!-- 없으면 "없음" -->
