# 과제 초안 전체 (Issue 발행용)

> 이 문서를 보고 GitHub Issue 를 하나씩 만든다. 제목과 본문을 그대로 복사하면 된다.
> 총 **21개 Issue** (P0: 4 / P1: 4 / P2: 6 / P3: 7)
>
> 발행 순서: P0 전부 → 나머지는 Phase 시작 시점에 발행.
> Projects 보드 컬럼: `대기 / 진행 / 검토 / 완료`

---

# Phase 0 — 공통 (W1: 8/03–8/09)

**이 단계는 3명이 같은 일을 각자 한다.** 분담하지 않는다.
목적은 결과가 아니라 **전원이 툴을 쓸 줄 알게 되는 것**이다.

---

### `#1` [전원] 베이스라인 논문 정독 및 파라미터표 확정

**라벨**: `setup` `phase-0` `전원`

**목적**
`baseline/params.yaml` 의 빈 항목을 논문 원문에서 확인해 채운다. 이 값이 틀리면 이후 25개 격자점이 전부 무의미해진다.

**할 일**

- [ ] [MDPI 2022 논문](https://www.mdpi.com/2072-666X/13/9/1476) 정독 (오픈 액세스)
- [ ] 아래 항목을 논문에서 찾아 `params.yaml` 에 기입
  - `geometry.fin_width_nm`
  - `geometry.junction_depth_nm`
  - `doping.channel_conc_cm3`
  - `doping.sd_peak_conc_cm3`
- [ ] 논문의 baseline Vth / SS / DIBL / Ion 값을 이 Issue 코멘트에 표로 정리 (나중에 우리 결과와 대조할 기준)
- [ ] 3명이 읽은 값이 서로 같은지 대조

**완료 판정**

- [ ] `params.yaml` 에 빈 항목이 없다
- [ ] `verified_by` 에 이름이 적혀 있다
- [ ] 논문 값 표가 코멘트에 올라와 있다

---

### `#2` [전원] 학교 라이선스 예제 폴더 조사

**라벨**: `setup` `phase-0` `최우선`

**목적**
DRAM/BCAT/리세스 게이트 예제가 있으면 W1 일정이 통째로 단축된다. **다른 무엇보다 먼저 한다.**

**할 일**

- [ ] Sentaurus 설치 경로의 Applications Library / examples 폴더 탐색
- [ ] 검색 키워드: `DRAM`, `BCAT`, `recess`, `saddle`, `buried`, `RCAT`, `cell transistor`
- [ ] 3D MOSFET / SDE Scheme 스크립팅 예제도 함께 목록화
- [ ] 찾은 예제 경로를 이 Issue 코멘트에 전부 적기
- [ ] 설치된 Sentaurus **버전**을 확인해 코멘트 (교차검증에서 필요)

**완료 판정**

- [ ] 예제 유무에 대한 명확한 답 (있으면 경로, 없으면 "확인 결과 없음")
- [ ] 3명의 Sentaurus 버전이 코멘트에 기록됨 — **버전이 다르면 즉시 팀 논의**

---

### `#3` [전원] BCAT 3D 구조 재현 (SDE)

**라벨**: `setup` `phase-0` `전원`

**목적**
`baseline/bcat_sde.scm` 의 `TODO-GEOM` 지점을 채워 실제로 도는 구조 스크립트를 만든다.

**할 일**

- [ ] `bcat_sde.scm` 을 자기 환경에서 실행
- [ ] `TODO-GEOM` 표시 지점을 논문 Fig.1 단면과 대조하며 수정
- [ ] 새들핀 형상이 실제로 만들어지는지 SVisual 로 확인
- [ ] 컨택 4개(gate/source/drain/substrate)가 제대로 정의되는지 확인
- [ ] **겹침부 국소 메쉬가 실제로 조밀해졌는지 확대해서 확인** (GIDL 신뢰성의 핵심)
- [ ] 단면 캡처를 이 Issue 에 드래그&드롭

**완료 판정**

- [ ] 에러 없이 메쉬 생성 완료
- [ ] 단면이 논문 그림과 육안으로 일치
- [ ] Lgate 20 nm, 리세스 깊이 120 nm 가 실제 구조에서 ±2 nm 이내
- [ ] 3명의 구조가 서로 같아 보인다

> **8/07(목)까지 이 과제가 안 끝나면** 즉시 팀 회의. 격자를 3×3(9점)으로 줄이는 안 검토.

---

### `#4` [전원] baseline 격자점 D36_N100 실행 및 3인 대조

**라벨**: `setup` `phase-0` `게이트`

**목적**
**W1 종료 게이트.** 3명이 독립적으로 같은 조건을 돌려 같은 값이 나오는지 확인한다.
여기서 안 맞으면 이후 등고선은 3명의 환경 차이를 그린 그림이 된다.

**할 일**

- [ ] `bcat_sdevice.cmd` 의 `TODO-MODEL` 확인 (설치 버전의 SDevice User Guide 대조)
- [ ] `D36_N100` 실행 → `runs/D36_N100/` 생성
- [ ] `plt2csv.py` 로 `idvg.csv` 생성. **데이터셋 이름을 팀에 공유**
- [ ] `extract.py` 로 지표 추출
- [ ] **BTBT on/off 비교** — 모델을 끄고 한 번 더 돌려 Vg<0 구간 전류 차이 확인
- [ ] 3명의 Vth / SS / GIDL 을 이 Issue 코멘트에 표로 나란히 정리
- [ ] 논문 값과 자릿수·경향 비교

**완료 판정 (전부 만족해야 W2 진행)**

- [ ] 3명의 Vth 가 서로 **5% 이내**
- [ ] Vth / SS / DIBL 이 논문 값과 같은 자릿수·경향
- [ ] BTBT 켠 것과 끈 것의 Vg<0 구간 전류가 **명확히 다르다** (같으면 모델이 안 켜진 것)
- [ ] `baseline/` 확정 PR 이 머지됨

---

### `#5` [전원] 분석 파이프라인 시험 (더미 데이터)

**라벨**: `setup` `phase-0` `쉬움`

**목적**
Sentaurus 결과를 기다리는 동안 extract → merge → contour 전체가 도는지 미리 확인.
**시뮬레이션이 다 끝난 뒤 스크립트가 안 돈다는 걸 알게 되는 상황을 막는다.**

**할 일**

```bash
pip install -r analysis/requirements.txt
python analysis/make_dummy_data.py
python analysis/extract.py --all
python analysis/merge.py
python analysis/contour.py --all-figures
python analysis/make_dummy_data.py --clean   # ★ 반드시 삭제
```

**완료 판정**

- [ ] `contour.py` 의 판정이 **"(b) 시너지"** 로 나온다 (더미에 시너지를 심어둠)
- [ ] `analysis/figures/contour_I_GIDL_A_um.png` 가 생성된다
- [ ] 더미를 지웠다 (`runs/` 에 `_template` 만 남음)

---

# Phase 1 — 1차원 단독 스윕 (W2: 8/10–8/16)

여기서부터 갈라진다.

---

### `#6` [A] DBCAT 단독 스윕 (도핑 N100 고정)

**라벨**: `sweep` `phase-1` `A`

**고정**: 도핑 배수 = 1.00 (baseline)
**스윕**: DBCAT = **24, 30, 36, 42, 48 nm** (5점)
**Run ID**: `D24_N100`, `D30_N100`, `D36_N100`, `D42_N100`, `D48_N100`

**이 과제가 왜 중요한가**
**8/16 체크포인트의 판단 근거가 이 결과다.** DBCAT 의 GIDL 주효과가 미미하면 2차원 격자를 다 돌리기 전에 방향을 바꿔야 한다.

**할 일**

- [ ] 5개 격자점 실행 (`D36_N100` 은 Phase 0 결과 재사용 가능)
- [ ] `extract.py --all` → `merge.py`
- [ ] **DBCAT vs log10(GIDL) 1차원 그래프**를 이 Issue 에 첨부
- [ ] 24 nm 극단값에서 구조 생성이 실패하지 않는지 확인 (질화막이 얇을 때)

**완료 판정 / 체크포인트 판단**

- [ ] 5점 전부 수렴
- [ ] **판단**: DBCAT 24→48 nm 에서 GIDL 이 **0.3 dex(약 2배) 이상** 변하는가?
  - 예 → W3 그대로 진행
  - 아니오 → 스윕 범위를 20~60 nm 로 확대해 재시도. 그래도 미미하면 기획서 5-4절 시나리오 B로 전환

---

### `#7` [B] 접합 도핑 단독 스윕 (DBCAT D36 고정)

**라벨**: `sweep` `phase-1` `B`

**고정**: DBCAT = 36 nm (공칭)
**스윕**: 도핑 배수 = **0.30, 0.50, 0.70, 0.85, 1.00** (5점)
**Run ID**: `D36_N030` ~ `D36_N100`

**할 일**

- [ ] 5개 격자점 실행
- [ ] **도핑배수 vs log10(GIDL)** 그래프 첨부
- [ ] **도핑배수 vs Ion** 그래프도 함께 첨부 — 저도핑의 대가가 여기서 보인다
- [ ] 저도핑 극단(×0.30)에서 Ion 손실이 얼마나 큰지 수치로 정리

**완료 판정**

- [ ] 5점 전부 수렴
- [ ] GIDL 이 도핑 저감에 따라 단조 감소하는가? (안 그러면 접합 프로파일 정의를 의심)
- [ ] Ion 손실률을 이 Issue 에 명시

---

### `#8` [C] BTBT 모델 3종 비교

**라벨**: `setup` `phase-1` `C` `발표근거`

**목적**
발표 Q&A "왜 그 모델을 썼나요?"에 대한 답을 **실험으로** 만든다. 심사 '완성도 30점'에 직결.

**고정**: `D36_N100` 구조
**스윕**: BTBT 모델 = **NonlocalPath / Hurkx / Schenk**
**Run ID**: `D36_N100_nonlocal`, `D36_N100_hurkx`, `D36_N100_schenk`

**할 일**

- [ ] 3종 각각 실행
- [ ] 세 Id-Vg 를 **한 그래프에 겹쳐** 그리기 (Vg<0 구간 확대)
- [ ] Vg=-0.5 V 에서의 GIDL 값을 표로 비교
- [ ] 모델별 수렴 난이도·실행 시간도 기록
- [ ] **"우리는 X를 쓴다, 근거는 Y"** 를 1페이지로 정리 → `docs/model_choice.md`

**완료 판정**

- [ ] 3종 결과 비교 그래프
- [ ] 선택한 모델과 그 근거가 문서화됨
- [ ] 선택 결과를 `baseline/bcat_sdevice.cmd` 에 반영하는 PR (2인 승인 필요)

---

### `#9` [C] 메쉬 수렴성 검증

**라벨**: `setup` `phase-1` `C` `발표근거`

**목적**
"GIDL 은 겹침부에 극도로 국소화된 현상이라 메쉬가 성기면 결과가 왜곡된다"는 기획서 5-1절의 우려를 **정량적으로 해소**한다.

**고정**: `D36_N100`, NonlocalPath
**스윕**: 겹침부 국소 메쉬 밀도 = **성김 / 중간(baseline) / 촘촘** 3단계
**Run ID**: `D36_N100_mesh_coarse`, `D36_N100_mesh_base`, `D36_N100_mesh_fine`

**할 일**

- [ ] `bcat_sde.scm` 의 `RS.Overlap` 값을 3단계로 바꿔 실행
- [ ] 각 조건의 노드 수와 실행 시간 기록
- [ ] **메쉬 밀도 vs GIDL 수렴 그래프**
- [ ] baseline 메쉬가 충분한지 판정

**완료 판정**

- [ ] 중간→촘촘에서 GIDL 변화가 **5% 이내**면 baseline 메쉬가 충분하다고 결론
- [ ] 5% 초과면 → baseline 메쉬를 촘촘으로 올리는 PR (2인 승인). 실행 시간 증가분도 함께 보고

---

# Phase 2 — 2차원 DoE (W3: 8/17–8/23)

격자점 하나당 Issue 를 만들면 25개가 되어 관리가 안 된다.
**열 단위로 묶어 6개 Issue** 로 발행한다. 체크박스로 개별 격자점을 추적한다.

---

### `#10` [A] D24 열 (5점)

**라벨**: `sweep` `phase-2` `A`

**고정**: DBCAT = 24 nm
**스윕**: 도핑 배수 = 0.30 / 0.50 / 0.70 / 0.85 / 1.00

- [ ] `D24_N030`
- [ ] `D24_N050`
- [ ] `D24_N070`
- [ ] `D24_N085`
- [ ] `D24_N100`  ← Phase 1 `#6` 결과 재사용 가능

**완료 판정**: 5점 전부 `metrics.csv` 에 nan 없음 / `merge.py` 진행률에 반영됨

---

### `#11` [A] D30 열 (5점)

**고정**: DBCAT = 30 nm

- [ ] `D30_N030` / `D30_N050` / `D30_N070` / `D30_N085` / `D30_N100`

---

### `#12` [B] D36 열 (5점)

**고정**: DBCAT = 36 nm (공칭)

- [ ] `D36_N030` ~ `D36_N100`  ← Phase 1 `#7` 결과 전부 재사용 가능

**추가 할 일**: 이 열은 공칭이므로 **논문 값과 직접 대조**해 코멘트로 보고

---

### `#13` [B] D42 열 (5점)

**고정**: DBCAT = 42 nm

- [ ] `D42_N030` ~ `D42_N100`

---

### `#14` [C] D48 열 (5점)

**고정**: DBCAT = 48 nm

- [ ] `D48_N030` ~ `D48_N100`

**추가 할 일**: 질화막이 가장 두꺼운 조건. 게이트 에치백이 과하면 **온전류가 급감**할 수 있다. Ion 을 특히 주의해서 볼 것.

---

### `#15` [전원] 교차검증 3점

**라벨**: `verification` `phase-2` `필수`

**목적**
3명의 환경이 실제로 동일한지 확인. **이걸 안 하면 등고선의 굴곡이 물리인지 환경차인지 구분할 수 없다.**

| 담당 | 추가 실행 | 대조 상대 |
|---|---|---|
| A | `D36_N100` | B |
| B | `D24_N100` | A |
| C | `D42_N100` | B |

**할 일**

- [ ] 각자 지정된 1점을 추가 실행 (`runs/D36_N100_xcheckA` 처럼 접미사)
- [ ] `python analysis/merge.py` 실행 → 교차검증 섹션 확인
- [ ] 결과를 이 Issue 에 표로 정리

**완료 판정**

- [ ] 3쌍 모두 GIDL 편차 **5% 이내**
- [ ] 초과 시 → 원인 규명 전까지 **등고선을 그리지 않는다.** 버전/정규화 폭/모델 파라미터를 순서대로 대조

---

# Phase 3 — 분석·발표 (W4: 8/24–8/31)

---

### `#16` [A] GIDL 2차원 등고선 — 헤드라인 그림

**라벨**: `analysis` `phase-3` `A` `발표핵심`

**할 일**

- [ ] `python analysis/merge.py` — 격자 100% 확인
- [ ] `python analysis/contour.py`
- [ ] 등고선이 기획서 4-4절의 (a)/(b)/(c) 중 어느 패턴인지 육안 판정
- [ ] 발표용으로 축 라벨·폰트 크기 조정 (뒷자리에서 보이게)

**완료 판정**

- [ ] `contour_I_GIDL_A_um.png` 생성
- [ ] 25개 격자점이 전부 그림 위에 점으로 표시됨
- [ ] 육안 판정 결과가 `contour.py` 의 회귀 판정과 **일치**한다 (불일치하면 데이터 이상)

---

### `#17` [B] Ion / DIBL / SS 등고선 + 트레이드오프 그림

**라벨**: `analysis` `phase-3` `B`

**할 일**

- [ ] `python analysis/contour.py --all-figures`
- [ ] **가장 중요한 그림**: x축 = GIDL 저감률, y축 = Ion 손실률, 점 = 25개 격자점
      → "Ion 손실 없이 GIDL 만 줄인 조건"이 좌하단에 보이면 그게 최고의 결과
- [ ] Pareto front(더 나아질 수 없는 경계) 표시

**완료 판정**

- [ ] 트레이드오프 산점도 완성
- [ ] "GIDL X% 저감 시 Ion 손실 Y%" 를 수치로 제시

---

### `#18` [C] 교호작용 회귀 및 시너지 정량화

**라벨**: `analysis` `phase-3` `C` `발표핵심`

**할 일**

- [ ] `python analysis/contour.py` 의 회귀 출력 확보
- [ ] 교호작용항의 t값 / p값 / 주효과 대비 크기 정리
- [ ] 시너지 표(개별 최적화 합 vs 결합 최적화)를 발표용 표로 정리
- [ ] 기획서 5-4절 시나리오 **A / B / C 중 어디인지 확정**

**완료 판정**

- [ ] 아래 문장의 빈칸이 전부 채워짐 (기획서 6-1절)

> "BCAT 의 DBCAT 과 Elevated S/D 접합 도핑은 [독립적이지 않으며 / 독립적이며],
> 결합 설계 시 개별 최적화 대비 GIDL 을 추가로 [ X ]% 저감할 수 있다.
> 이를 통해 [DBCAT = __ nm, 도핑 = ×__] 영역이 리텐션 관점에서 최적임을 제시한다."

---

### `#19` [C] 2차원 설계 맵 작성

**라벨**: `analysis` `phase-3` `C` `독창성`

**목적**
**개별 논문에는 없는 형태의 결과물.** 심사 '독창성 30점'의 실체.

**할 일**

- [ ] "DBCAT 이 D 일 때 최적 접합 조건은 X" 형태의 표 작성
- [ ] 각 DBCAT 값에 대해 GIDL 최소가 되는 도핑 배수를 찾아 표기
- [ ] 그 최적선이 등고선 위에서 **직선인지 휘었는지** 표시 → 휘었으면 트레이드오프의 시각적 증거

---

### `#20` [전원] 한계·향후 과제 정리

**라벨**: `analysis` `phase-3` `전원`

**목적**
기획서 6-2절. **한계를 정직하게 밝히는 것은 감점이 아니라 신뢰도 가점 요인이다.**

**반드시 포함**

- [ ] 통계적 변동성(공정 산포)을 다루지 않았음 — 결정론적 설계 최적화에 집중
- [ ] 3D 어레이 레벨 효과(인접 워드라인 간섭) 미고려
- [ ] 에치백 공정의 비평탄성 미반영
- [ ] Row Hammer 는 문헌 기반 서술적 연결이며 직접 시뮬레이션하지 않았음
- [ ] 격자 해상도 5×5 의 한계 (더 미세한 상호작용 구조는 놓칠 수 있음)

---

### `#21` [전원] 발표자료 작성

**라벨**: `presentation` `phase-3` `전원`

**서사 구조** (기획서 6-3절, 10분 기준)

| 단계 | 내용 | 비중 | 담당 |
|---|---|---|---|
| 문제 제기 | BCAT 겹침부에서 GIDL 이 왜 생기는가 (그림 중심) | 20% | |
| 기존 접근 | DBCAT / 접합 저도핑 — 문헌 정직하게 인용 | 15% | |
| 우리 질문 | 둘을 함께 하면? 독립인가? | 10% | |
| 방법 | 2차원 DoE, baseline 캘리브레이션, **3인 교차검증** | 15% | |
| 결과 | 2차원 등고선 — 시각적 하이라이트 | 30% | |
| 결론 | 설계 맵, Row Hammer 연결, 한계 | 10% | |

**예상 질문 대비** (기획서 6-4절)

- [ ] "이거 그냥 파라미터 최적화 아닌가요?"
- [ ] "실제 공정에서 DBCAT 을 제어할 수 있나요?"
- [ ] "DBCAT 이 GIDL 에 영향이 없으면?"
- [ ] "공정 산포는 고려했나요?"
- [ ] "왜 GAA 가 아니라 DRAM 인가요?"
- [ ] **"왜 그 BTBT 모델을 썼나요?"** → `#8` 결과로 답
- [ ] **"메쉬는 충분한가요?"** → `#9` 결과로 답
- [ ] **"3명이 돌린 결과가 같다는 보장이 있나요?"** → `#15` 교차검증으로 답
