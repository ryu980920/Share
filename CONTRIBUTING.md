# 작업 규칙

> 이 문서의 규칙은 **결과 자동 병합을 위한 것**이다. 형식이 어긋나면 `merge.py` 가 그 격자점을 통째로 누락시킨다.

---

## 1. 절대 규칙 3가지

| # | 규칙 | 이유 |
|---|---|---|
| 1 | `baseline/` 은 직접 수정 금지. PR + 나머지 2인 승인으로만 변경 | 3명의 결과가 비교 가능하려면 기준이 하나여야 함 |
| 2 | 지표(Vth·SS·DIBL·GIDL)는 **손으로 계산하지 않는다**. 반드시 `analysis/extract.py` 사용 | Vth 정의가 사람마다 다르면 등고선이 무의미해짐 |
| 3 | 자기 담당 격자점 폴더 외에는 건드리지 않는다 | 충돌 방지 |

---

## 2. 명명 규칙 (Run ID)

```
D{DBCAT_nm}_N{도핑배수×100, 3자리}
```

| 예시 | 의미 |
|---|---|
| `D24_N030` | DBCAT 24 nm, 접합 도핑 = 베이스라인 × 0.30 |
| `D36_N100` | DBCAT 36 nm (공칭), 도핑 = 베이스라인 × 1.00 → **이것이 베이스라인 격자점** |
| `D48_N085` | DBCAT 48 nm, 도핑 × 0.85 |

부가 실험은 접미사를 붙인다: `D36_N100_mesh2`, `D36_N100_hurkx`

---

## 3. 파일 규격

### `runs/<RUN_ID>/idvg.csv` — 원시 데이터 (필수)

컬럼명을 **정확히 이대로** 쓴다. 대소문자 구분.

```csv
Vg,Id_lin,Id_sat
-1.0,3.12e-13,8.45e-11
-0.9,2.01e-13,5.22e-11
...
```

- `Vg` : 게이트 전압 [V]. **-1.0 V부터 +2.8 V까지, 0.05 V 간격**
- `Id_lin` : Vd = 0.1 V 에서의 드레인 전류 [A/µm]
- `Id_sat` : Vd = 1.0 V 에서의 드레인 전류 [A/µm]
- 전류는 **폭으로 정규화된 A/µm** 단위. 정규화 폭은 `baseline/params.yaml` 의 `device_width_um` 값을 쓴다.

> Sentaurus `.plt` 를 이 형식으로 바꾸는 방법은 `analysis/README.md` 참고.

### `runs/<RUN_ID>/run.yaml` — 메타데이터 (필수)

`runs/_template/run.yaml` 을 복사해서 채운다.

### `runs/<RUN_ID>/metrics.csv` — 추출 지표 (필수)

**직접 만들지 않는다.** 아래 명령이 생성한다.

```bash
python analysis/extract.py runs/D36_N100
```

### `runs/<RUN_ID>/README.md` — 3줄 요약 (필수)

- 무엇을 바꿨는가
- 무엇이 나왔는가 (핵심 숫자 1개)
- 이상한 점 / 의심되는 점

---

## 4. 브랜치 & PR

```bash
git checkout -b run/D36_N100        # 격자점 작업
git checkout -b fix/baseline-mesh   # baseline 수정
git checkout -b docs/devlog-0810    # 문서
```

- PR 제목: `[A] D24 열 5점 완료` 처럼 **담당자 이니셜을 대괄호로** 시작
- 리뷰어: 격자점 PR은 1명, `baseline/` PR은 2명 승인 필수
- **머지 후 브랜치 삭제**

---

## 5. 커밋하지 말 것

`.gitignore` 에 걸려 있지만 다시 강조한다.

| 금지 | 이유 | 대안 |
|---|---|---|
| `.tdr`, `.grd`, `.dat`, `.plt` 원본 | 수백 MB. 저장소가 망가짐 | 추출한 `idvg.csv` 만 올림 |
| Workbench 프로젝트 폴더 전체 | 위와 같음 | 커맨드 파일만 |
| 스크린샷 남발 | 용량 | 결론 플롯 1~2장만 |

구조 확인용 SVisual 캡처는 **PR 코멘트에 드래그&드롭**으로 붙인다. 저장소에 커밋하지 않아도 팀원이 볼 수 있다.

---

## 6. 막혔을 때

1. 30분 넘게 같은 에러면 **바로 Issue에 코멘트**로 올린다. 혼자 붙잡고 있지 않는다.
2. 에러 로그는 접기(`<details>`)로 감싸서 전체를 붙인다. 요약하지 말고 원문 그대로.
3. `prompts/04_트러블슈팅.md` 프롬프트를 AI에 넣으면 형식에 맞는 질문이 생성된다.
